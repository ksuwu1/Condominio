<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Condominio" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Cambiar el color de la barra de estado a verde_medio -->
        <item name="android:statusBarColor">@color/verde_medio</item>

        <!-- Configurar los iconos oscuros para la barra de estado (opcional) -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Cambiar el color de la barra de navegación (inferior) -->
        <item name="android:navigationBarColor">@color/verde_claro</item>
    </style>

</resources>